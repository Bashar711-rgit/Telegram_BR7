# 🤖 Telegram Bot v13.0 – IntentEngine Edition (Render Cloud)

نظام متقدم لمراقبة مجموعات تيليجرام واصطياد طلبات المساعدة الطلابية، مهيأ للتشغيل **24/7 على Render المجاني** كـ Web Service.---

## 🆕 v9.33 — إصلاح إدارة الجلسات + الحسابات مبنية على قاعدة البيانات (Master Prompt 4)

1. **السبب الجذري لـ«تعذر تحميل الحسابات: Invalid token»:** صفحة `/login` كانت تقرأ التوكن من مفتاح localStorage مختلف (`dash_token`) عن مفتاح اللوحة الرئيسية (`dashboard_token`) — فتبدأ الصفحة بلا توكن أو بتوكن قديم فيسقط كل نداء بـ401. أُصلح: الصفحتان تشتركان الآن في `dashboard_token` (مع قراءة احتياطية للمفتاح القديم للهجرة) — **صفر تغيير على أي نص في الواجهة**.
2. **جدول `dashboard_accounts` في قاعدة البيانات** (prefix فريد، name، api_id/api_hash، phone، session_name، priority، session_string، is_main، enabled، origin): مصدر حقيقة اللوحة للحسابات — أي إضافة/تحديث/تسجيل جلسة يُخزَّن فعلياً.
3. **POST /api/accounts أعيد بناؤه (Frontend → API → Backend → DB → Render → Response):** قاعدة البيانات أولاً (فشلها = فشل الطلب الصريح 500 — لا نجاح وهمياً) → مزامنة متغيرات بيئة Render في **نداء دفعة واحد** (`render_upsert_env_many`؛ سقوط رشيق لرفع فردي عند عدم دعم الدفعة — مُختبر حياً: PUT الدفعة 200 على هذه الخطة) → إلحاق accounts.env للتشغيل المحلي فقط (قرص Render مؤقت؛ الديمومة من متغيرات البيئة). الترقيم يختار أول ACCOUNT_N حر على الاتحاد، والسقف 20 محسوب على الاتحاد كله، ومنع تكرار الهاتف يغطي البيئة وقاعدة البيانات معاً.
4. **المصدر الموحد `_merged_accounts`:** ACCOUNTS (بيئة الإقلاع = التكوين النشط) ∪ جدول قاعدة البيانات (حسابات بانتظار النشر) — **GET /api/login/accounts** و**GET /api/accounts** يعرضان الاتحاد: الحساب المضاف يظهر في القائمة المنسدلة **فوراً دون إعادة تشغيل** بعلامة `pending_deploy` وبصفوف صفرية صادقة (لا اختفاء ولا بيانات وهمية)، وفشل قاعدة البيانات ينهار رشيفاً إلى بيئة الإقلاع فلا تنكسر صفحة الجلسات أبداً.
5. **send-code من قاعدة البيانات:** البحث في المصدر الموحد — الحساب غير المنشور يستقبل رمز التحقق أيضاً لأن LoginManager يحتاج api_id/api_hash/phone فقط لا مراقباً حياً (مُختبر: نداء مُحاكى يصل ببيانات صف قاعدة البيانات).
6. **نجاح التسجيل يُحدّث كل المخازن:** Session String → متغير بيئة Render (ديمومة + إعادة نشر تلقائية — السلوك الحالي كما هو) + صف قاعدة البيانات (حالة اللوحة) + نسخة الإقلاع في الذاكرة (الشارة «موجودة» فوراً). الجلسة لا تُعاد في أي استجابة أبداً (H-4، مُختبر).
7. **تنظيف:** حذف تعريف `_audit` المكرر (الثاني كان يطغى على الأول صمتاً) — السلوك مطابق حرفياً.
8. **تدقيق الديناميكية الشامل:** الإعدادات (app_settings) والكلمات المفتاحية (نسخة DB مع استعادة عند الإقلاع عبر dashboard_store) والقواعد والمصادر والسماح/الحظر والتدقيق والإشعارات والميزات والمستخدمون والرسائل والتنبيهات — كلها كانت وأصبحت مؤكدة قاعدة-بيانية؛ الفجوة الوحيدة كانت الحسابات وأُغلقت هنا.
9. **14 اختباراً جديداً** (`test_accounts_db.py`): الحفظ الحقيقي في DB، لا نجاح وهمي عند فشل DB، القوائم من المصدر الموحد، منع التكرار، السقف 20 على الاتحاد، الترقيم التخطي، send-code من DB، حفظ الجلسة عند النجاح، عدم تسريب الجلسة، وانحصار مفتاح التوكن في القالب — **إجمالي 460**.

---

## 🆕 v9.32 — توسيع RBAC على كل نقاط BotPanel (Master Prompt 9 كامل المفعول)

1. **47 مساراً انتقلت من `verify_token` (الرئيسي فقط) إلى `require_permission()` بصلاحية دقيقة** — كل نقطة مربوطة بأقرب صلاحية من مصفوفة v9.29: الحسابات `accounts.*`، المصادر `sources.*`، الكلمات `keywords.*`، القواعد/قوائم الحظر/السماح `rules.*`، الرسائل والتنبيهات والتحليلات `messages.read`، الإشعارات `notifications.read`، الميزات والإعدادات والتطهير `settings.*`، النسخ الاحتياطي `backup.*`، التدقيق `audit.read`، إعادة التشغيل ورمز الحساب `deployment.execute`/`accounts.write`.
2. **`/api/stats` → `auth.read`** (أدنى صلاحية يشترك فيها كل الأدوار) — لأن شاشة الدخول تتحقق من التوكن عبره؛ بقي عمله لكل الأدوار.
3. **استثناءات مقصودة موثقة:** `/health/full` و`/ws` احتفظا بـ`verify_token` (عمق تشخيصي وقناة بث — ليست نقاط وظائف إدارية)، و`/api/auth/*` و`/api/users*` كما هي.
4. **التمييز الدقيق بين الأدوار صار فعلياً على كل اللوحة:** viewer يقرأ لكن لا يكتب ولا يرى الإعدادات/النسخ الاحتياطي؛ operator لا يرى القواعد (لا `rules.read`)؛ supervisor يقرأ الإعدادات والنسخ لكن لا `deployment.execute`؛ admin وحده يعيد التشغيل ويضيف الحسابات؛ super_admin (الرئيسي) مطلق كما كان — **صفر تغيير على مسارات التوكن الرئيسي**.
5. **حارس القفل نفسه يغطي المسارات الموسعة** (لا باب جانبي): توكن مستخدم فاسد يُحتسب، و403 لا يُحتسب فشلاً (نجاح المصادقة سابق للفحص).
6. **11 اختباراً جديداً** (`test_rbac_expansion.py`): مصفوفة تمييزية لكل دور على قراءات وكتابات تمثيلية + حدود التوسيع المقصودة (health/full) + الرئيسي المطلق — **إجمالي 446**.

---

## 🆕 v9.31 — لوحة GitHub في مركز التحكم (Master Prompt 6.13)

1. **بطاقة «🐙 حالة GitHub»** في تبويب مركز التحكم (webadmin): المستودع + الفرع + آخر commit (sha/رسالة/مؤلف) + آخر 5 commits + شارة التزامن «الكود المنشور محدّث» بمقارنة HEAD المحلي مع GitHub.
2. **علاقة النشر موثقة في البطاقة:** الدفع إلى الفرع الرئيسي يشغّل النشر التلقائي على Render — القارئ يفهم السبب-النتيجة دون فتح GitHub.
3. **اعتماد كامل على متغيرات البيئة** (Master Prompt 2.1 حرفياً): `GITHUB_TOKEN` + `GITHUB_REPO` (يُكتشف تلقائياً من origin عند الغياب) — عند عدم الضبط تعرض البطاقة تعليمات الإعداد بدل أخطاء، وعند رفض التوكن (401) تظهر رسالة تحديث واضحة.
4. **`webadmin/github_api.py`:** عميل GitHub مصغر async بأسلوب render_api.py — تدهور رشيق، بلا تسريبات (لا يُطبع التوكن ولا يعود في أي استجابة).
5. اختباران جديدان: حالة git المحلية (sha/فرع/repo) + قاموس «غير مضبوط» الصحيح بلا توكنات — 432/432.

---

## 🆕 v9.30 — واجهة مركز الإشعارات + مولدات الأحداث (إكمال v9.24)

1. **جرس BotPanel 🔔 + شارة حية:** عدّاد غير المقروء يُبَث في إطار WS stats كل دورة (`notifications_unread` — كاش 5 ثوانٍ، فشل-آمن تماماً لا يعطّل البث)، تحديث فوري بلا Refresh.
2. **لوحة منسدلة كاملة:** آخر 30 إشعاراً، نقاط شدة ملوّنة (critical أحمر / warn كهرماني / info أزرق)، وقت نسبي عربي، تعليم فردي بالضغط + «تعليم الكل»، إغلاق بالنقر خارجها.
3. **مولدات إشعارات جديدة على الأحداث:** `bot.restart` (critical) عند إعادة التشغيل من BotPanel + `data.restore` (warn) عند استعادة نسخة من webadmin — مع القفل (v9.28) والمستخدم الجديد (v9.29) وتبديل الميزات (v9.25).
4. الكل خلف بوابة الميزة `notifications` (v9.25): إيقافها يصمت الجرس بالكامل.
5. التحقق: 430/430 اختباراً + E2E بالمتصفح (زر الجرس، فتح اللوحة، شارة حية بعد حدث قفل).

---

## 🆕 v9.29 — P4 RBAC: مستخدمو اللوحة والأدوار (Master Prompt 8/9)

1. **جدول `dashboard_users`:** حسابات بشرية بأدوار خمسة (Master Prompt 9.1 حرفياً): `super_admin` / `admin` / `supervisor` / `operator` / `viewer` — كلمات المرور scrypt-hashed (معاملات موثقة في رأس السلسلة، تحقق timing-safe) ولا تخرج الـhash في أي استجابة.
2. **صلاحيات دقيقة Granular (9.2):** `accounts.*` / `sources.*` / `keywords.*` / `rules.*` / `settings.read|write` / `deployment.read|execute` / `audit.read` / `users.read|write` + `users.delete` و`super_admin`-grant للمدير الأقصى فقط.
3. **التوكن الرئيسي يبقى مفتاح Super Admin المطلق** (`*`) — صفر تغيير على المسارات الحالية وكل الاختبارات التاريخية تنجو دون تعديل.
4. **`POST /api/auth/login`:** اسم/كلمة مرور → توكن مستخدم موقّع HMAC-SHA256 (payload.signature، صلاحية 12 ساعة) مشتق من التوكن الرئيسي — تغييره يبطل كل الجلسات. فشل الدخول يغذي **نفس حارس قفل v9.28** (لا باب جانبي للتخمين) والقفل يُدوَّن `auth.lockout`.
5. **`GET /api/auth/me`** للهوية + `GET/POST/PATCH/DELETE /api/users` لإدارة كاملة (تفعيل/تعطيل، دور، كلمة مرور، حذف ناعم) مع حماية الذات: لا تعطيل/حذف/تخفيض حسابك أثناء استخدامه.
6. **حذف ناعم (Soft Delete — 10.2):** `deleted_at` + `enabled=0` — الصف يبقى للسجل التدقيقي ولا يعود صالحاً للدخول إطلاقاً.
7. **التدقيق:** `user.create` / `user.update` / `user.delete` بفاعل `botpanel-admin` (master) أو `panel:<username>` (مستخدم) + `user.login` عند كل دخول + إشعار info عند إنشاء مستخدم.
8. **تبويب «🔐 المستخدمون والأدوار» في webadmin:** إحصاءات حية، نموذج إنشاء بوصف الدور، شارات أدوار ملوّنة، تبديل دور/تفعيل فوري، إعادة كلمة مرور، حذف ناعم بتأكيد — كل عملياته عبر جلسة webadmin المحمية بـCSRF ومُدقَّقة `user.*` من المصدر webadmin.
9. **38 اختباراً جديداً** (test_rbac.py): تشفير، توكنات (انتهاء/تزوير/دور مجهول)، RBAC (403 للعارض، 403 لمنح super_admin من admin)، حماية الذات، قفل متكامل، انعكاس تدقيقي، انحدار master.

---

## 🆕 v9.28 — سياسات احتفاظ قابلة للضبط + حارس قفل ضد تخمين التوكن

1. **سياسات الاحتفاظ:** ثلاثة إعدادات حية في مجموعة «🗄️ سياسات الاحتفاظ» — رسائل/تدقيق/إشعارات. الافتراضيات = السلوك التاريخي حرفياً (7/90/30)، وتعديلات اللوحة تنطبق دون إعادة تشغيل. حد أدنى 30 يوماً للتدقيق عمداً لحماية الأثر التدقيقي.
2. **حارس قفل المحاولات:** عدّاد فشل لكل IP (أول قيمة X-Forwarded-For خلف البروكسي) — 10 فشلات خلال 5 دقائق = قفل 60 ثانية يتضاعف أُسّياً حتى سقف 15 دقيقة (لا حظر دائم). نجاح واحد بتوكن صحيح يمسح الحالة فوراً. العتبات عبر env (AUTH_FAIL_THRESHOLD وغيرها).
3. **429 + Retry-After** للـIP المقفل قبل أي مقارنة توكن، والطلبات بلا ترويزة تُرفض قبل الحارس فلا تُحتسب — الحارس يستهدف تخمين التوكن الفعلي فقط. التوكن الخاطئ عبر `/ws` يُحتسب في نفس العدّاد (لا باب جانبي).
4. **القفل يُدوَّن** `auth.lockout` في سجل التدقيق (الممثل ثابت والمـIP في object_id) + إشعار critical للمشرف.
5. **شاشة الدخول:** زر «دخول» يتحقق فعلياً من التوكن عبر `/api/stats` قبل إغلاق النافذة، ورسالة قفل كهرمانية بعدّ تنازلي حي، وأي 429 أثناء الاستخدام يعيد لشاشة الدخول بالعدّاد.
6. `verify_token` صار async — إصلاح انسياب جدولة مهام التدقيق في threadpool.

---
## 🆕 v9.25 — سجل الميزات (Feature Registry) + ترقية مركز الإشعارات

1. **سجل الميزات:** 5 مفاتيح قابلة للتبديل اللحظي من تبويب «🧩 الميزات»: `sources_scope` · `allowlist` · `rule_engine` · `notifications` · `account_health`. المفاتيح والافتراضات في الكود، والجدول يخزّن الإزاحات فقط.
2. **فشل-آمن شامل:** مفتاح غير معروف أو خلل قاعدة بيانات = الميزة مفعّلة — لا انكسار للفلترة أبداً. كاش 30 ثانية + إبطال فوري عند التبديل.
3. **بوابة إشعارات واحدة:** داخل `record_notification` تغطي اللوحتين معاً — إيقاف الميزة يمنع أي إشعار جديد حتى من تبديل الميزات نفسها (سلوك مقصود).
4. **API:** `GET /api/features` + `POST /api/features/{key}/toggle` (404 خارج السجل) — كل تبديل مدوَّن `feature.toggle` ويولّد إشعاراً عند الإيقاف.
5. **تبويب الميزات:** بطاقات بمفاتيح حقيقية (role=switch + aria) وتأكيد إيقاف يشرح أثر الميزة تحديداً ووسم «معدّلة يدوياً».
6. **مركز الإشعارات (طبقة البيانات):** جدول `notifications` + دوال فشل-آمنة (تطبيع الشدة، قصّ الحقول، تعليم مقروء/الكل، تقليم 30 يوماً) + `GET /api/notifications` و`POST read` و`POST read-all`.

---
## 🆕 v9.19 — سجل التدقيق + حماية البيانات (P0)

1. **سجل التدقيق (Audit Log):** كل عملية كتابة من لوحتي البوت (BotPanel وwebadmin) تُسجَّل في جدول `audit_logs` — الفاعل، الإجراء، الكائن، القيم قبل/بعد، والمصدر. بلا أسرار والهواتف مُقنَّعة، والكتابة fire-and-forget فلا تُعطّل أي عملية.
2. **نقطة فحص `GET /api/audit`** + تبويب «🛡 التدقيق» في اللوحة بفلتر إجراءات وعداد وقيم قبل/بعد بتلميحات.
3. **نسخة أمان قبل الاستعادة:** أي استعادة نسخة احتياطية من webadmin تنشئ نسخة أمان تلقائية أولاً وتعيد اسمها في الاستجابة — التراجع ممكن دائماً.
4. **حد 20 حساباً** في `POST /api/accounts` (409 عند التجاوز) — توازٍ مع webadmin.
5. تقليم تلقائي لسجل التدقيق بعد 90 يوماً ضمن حلقة التنظيف الدورية.

---

## 🆕 تحديثات v9.13.1 — تشخيص الحسابات الصامت

**المشكلة:** كان `load_accounts()` يُتخطى أي حساب لا يملك `{prefix}_API_ID` **بدون أي تحذير** — حتى لو كانت بقية متغيرات الحساب (API_HASH / PHONE / SESSION_STRING) مضبوطة بالكامل. النتيجة: خدمة تعمل بـ **صفر حسابات** بينما كل Session Strings موجودة في البيئة، دون أي مؤشر في السجلات.

**الإصلاح (config.py فقط — لا تغيير في سلوك التشغيل):**

1. **تحذير التهيئة الجزئية:** عند تخطي حساب لغياب `_API_ID` مع وجود متغيرات أخرى له، يُسجل الآن تحذير صريح يعدّد المتغيرات الموجودة والمطلوبة بالضبط:
   ```
   Account MAIN SKIPPED: MAIN_API_ID is missing, but 2 other var(s) are set
   (MAIN_SESSION_STRING, MAIN_PHONE). Set MAIN_API_ID (and MAIN_API_HASH ...)
   ```
2. **تحذير فجوات الترقيم:** اكتشاف `ACCOUNT_1..20` يتوقف عند أول فجوة. إن وُجدت حسابات لاحقة مضبوطة (مثل ACCOUNT_3 بدون ACCOUNT_2) يُسجل تحذير بأن الترقيم يجب أن يكون متصلاً.

> 📌 **ملاحظة تشغيلية:** للتشغيل السحابي غير التفاعلي يلزم لكل حساب: `{prefix}_API_ID` + `{prefix}_API_HASH` + `{prefix}_PHONE` + `{prefix}_SESSION_NAME` (+ `{prefix}_SESSION_STRING` لتسجيل الدخول دون تفاعل). أي متغير ناقص سيظهر تحذيره في السجل عند الإقلاع مباشرة.

---

## 🆕 تحديثات v9.11

### المرحلة الأولى — أزرار تفاعلية أسفل كل تنبيه

أسفل كل رسالة تنبيه في القناة الهدف صفّان من الأزرار:

| الزر | النوع | الوظيفة |
|---|---|---|
| **عرض الرسالة** | URL | يفتح الرسالة الأصلية في مصدرها (t.me/{chat}/{msg} أو t.me/c/{id}/{msg} للمجموعات الخاصة — أفضل حل متاح ضمن Telethon) |
| **تواصل مع المرسل** | Inline | يعرض قائمة الرسائل الجاهزة كاملة — يختار المشرف واحدة فتُرسل فوراً إلى صاحب الطلب |
| **مراسلة** | URL | يفتح محادثة مباشرة مع المرسل (بدون أي إرسال تلقائي) |
| **📋 نسخ النص** | Inline | ميزة قائمة من v9.10 — نسخ نص التنبيه |

**الرسائل الجاهزة** (قابلة للتعديل عبر `CONTACT_TEMPLATES_JSON`) — 5 قوالب مدمجة (بحث / CV / واجبات × 2 / مشروع).

**مسار احتياطي** (يعمل دائماً حتى لو لم تصل أحداث الأزرار التفاعلية): رد على التنبيه بكلمة **«تواصل»** لإظهار القائمة، أو رد برقم **1-5** لإرسال الرسالة المختارة.

### المرحلة الثانية — نظام مكافحة المعلنين والسبام

```
Watch List (مراقبة 10 دقائق — لا حظر ولا تجاهل)
   │  شرط 1: رسالتان خلال 60 ثانية (أي مجموعات)
   │  شرط 2: رسائل متشابهة ≥ 80% خلال 5 دقائق
   │  شرط 3: أكثر من طلب من فئات مختلفة خلال 5 دقائق
   ▼
Spam Confirmation (أي شرط أثناء المراقبة)
   │  طلب جديد/معاد صياغته، رسالة إضافية خلال 5 دق،
   │  الانتقال لمجموعات أخرى بنفس الغرض، فئات متعددة،
   │  5 رسائل/10 دق أو 3 مجموعات/10 دق
   ▼
Permanent Ignore (Cross-Group Spam / Mass Poster)
      إيقاف كل التنبيهات + تجاهل كل الرسائل المستقبلية
      + تسجيل سبب الحظر بالتفصيل + ينجو من إعادة التشغيل

تصنيف مباشر (دون مراقبة): نفس الرسالة/المعنى في 4 مجموعات خلال 5 دقائق
```

- كل العتبات قابلة للتعديل الحي من لوحة التحكم (مجموعة «مكافحة السبام»).
- أوامر المشرف: `/spam` (تقرير القوائم) و `/unspam <id>` (إزالة من التجاهل) و `/contact` (عرض الرسائل الجاهزة).
- المستخدم تحت المراقبة يستمر في تلقي التنبيهات كالمعتاد — التأكيد فقط يوقفها.

### المرحلة الثالثة — إصلاح قاعدة البيانات (int32 overflow)

- `alert_dedup.sender_id` كان يبقى `INTEGER` (int4) في PostgreSQL → الآن `BIGINT`.
- ترحيل BIGINT شامل لكل أعمدة المعرفات (14 عموداً في 9 جداول) مع إعادة محاولة عند فشل الأقفال.
- فشل الترحيل لم يعد يُبتلع بصمت: سجل ERROR + تحقق فعلي من `information_schema` بعد الترحيل وتحذير CRITICAL عند أي عمود متبقٍ int4.
- جداول جديدة: `spam_watch` + `spam_ignore` (معرفات BIGINT من اليوم الأول).

---

## 🚀 أبرز التعديلات لنسخة Render

- **Web Service على الخطة المجانية** — منفذ واحد `$PORT` يخدم لوحة التحكم + `/health`.
- **صفحة تسجيل دخول مدمجة `/login`** — توليد Session String لكل حساب من المتصفح (OTP + 2FA) وحفظه تلقائياً في متغيرات Render عبر Render API مع إعادة نشر تلقائية.
- **دعم `*_SESSION_STRING`** — لا حاجة لملفات جلسة أو إدخال تفاعلي.
- **Keep-Alive ذاتي** — ping دوري كل 10 دقائق لمنع سبات الخطة المجانية (يُنصح أيضاً بربط UptimeRobot على `/health`).
- **إصلاح `keywords.json`** وإعادة تسمية `dashboard.py` للتوافق مع لينكس.

---

## 📂 هيكل المشروع

```
├── main.py                 # نقطة التشغيل الرئيسية
├── config.py               # مدير الإعدادات (+SESSION_STRING لكل حساب)
├── database.py             # طبقة قاعدة البيانات (SQLite/PostgreSQL)
├── filter_engine.py        # محرك الفلترة IntentEngine
├── monitors.py             # مراقبو الحسابات (StringSession)
├── dashboard.py            # لوحة التحكم + /health + /login
├── keywords.json           # قاعدة الكلمات المفتاحية
├── templates/dashboard.html
├── generate_session.py     # توليد Session String محلياً (احتياطي)
├── render.yaml             # Blueprint (بدون أسرار)
├── requirements.txt
├── runtime.txt             # python-3.11.9
└── .gitignore              # يستثني accounts.env والجلسات والأسرار
```

---

## ☁️ النشر على Render (الطريقة المعتمدة)

1. ارفع المشروع إلى GitHub (بدون `accounts.env` — محمي بـ `.gitignore`).
2. أنشئ **Web Service** جديداً (plan: free) واربطه بالمستودع:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python main.py`
   - Health Check Path: `/health`
3. أضف متغيرات البيئة من لوحة Render > Environment (انظر الجدول أدناه).
4. بعد تشغيل الخدمة افتح `https://<service>.onrender.com/login` وسجّل كل حساب برمز OTP — تُحفظ الجلسة تلقائياً ويُعاد النشر.
5. (موصى به) اربط [UptimeRobot](https://uptimerobot.com) بـ `/health` كل 5 دقائق لضمان عدم السبات.

> **ملاحظة:** قاعدة SQLite على الخطة المجانية مؤقتة (تُصفَّر عند إعادة النشر). للاستمرارية أنشئ PostgreSQL وأضف `DATABASE_URL`.

---

## 🔑 متغيرات البيئة المطلوبة

| المتغير | الوصف |
|---|---|
| `TARGET_GROUP_ID` | معرف المجموعة المستهدفة للتنبيهات |
| `ADMIN_CHAT_ID` | معرف شات الأدمن |
| `DASHBOARD_ENABLED` | `true` |
| `DASHBOARD_AUTH_TOKEN` | رمز قوي للوحة التحكم و`/login` |
| `SECRET_KEY_OVERRIDE` | مفتاح Fernet |
| `RENDER_API_KEY` | مفتاح Render API (لحفظ الجلسات تلقائياً من `/login`) |
| `RENDER_SERVICE_ID` | معرف الخدمة `srv-...` |
| `{PREFIX}_API_ID / _API_HASH / _PHONE / _SESSION_NAME` | بيانات كل حساب (MAIN, ACCOUNT_1..5) |
| `{PREFIX}_SESSION_STRING` | تُملأ تلقائياً عبر `/login` أو يدوياً |

متغيرات IntentEngine والأداء لها قيم افتراضية جاهزة (انظر `render.yaml`).

---

## 🔐 توليد Session String يدوياً (اختياري)

على هاتفك أو جهازك:

```bash
pip install telethon
python generate_session.py
```

ثم انسخ الناتج إلى `{PREFIX}_SESSION_STRING` في متغيرات Render.

---

## 📋 أوامر الأدمن (في شات الأدمن)

`/help` · `/stats` · `/status` · `/accounts` · `/health` · `/filter_stats` · `/dashboard` · `/block <id>` · `/unblock <id>` · `/purge` · `/spam` · `/unspam <id>` · `/contact`

---

## 🌐 لوحة التحكم

- `/` — لوحة التحكم الكاملة (تحتاج `DASHBOARD_AUTH_TOKEN`)
- `/login` — تسجيل دخول الحسابات وتوليد الجلسات
- `/health` — فحص الصحة (بدون مصادقة) لـ Render وUptimeRobot

---

📅 **الإصدار:** v13.0 – IntentEngine Edition + v9.11 (أزرار التواصل + مكافحة السبام + إصلاح BIGINT) – 2026-09
